# Google Vision credentials

이 폴더에는 Google Cloud Vision OCR용 서비스 계정 JSON 파일을 넣습니다.

예시:

```text
credentials/google-vision/service-account.json
```

이 폴더의 실제 credential 파일은 Git에 커밋되지 않도록 `.gitignore`에 등록되어 있습니다.
